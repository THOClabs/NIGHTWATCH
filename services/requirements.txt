# NIGHTWATCH Services Dependencies
# Version pinned for reproducible builds (updated January 2026)

# Ephemeris calculations (JPL DE440)
skyfield~=1.48

# Async HTTP client (weather station API)
aiohttp>=3.14.1,<4

# Serial communication (mount control)
pyserial~=3.5

# Device Communication (see docs/INTEGRATION_PLAN.md Phase 3)
# INDI client for Linux device control
pyindi-client~=2.0.8

# ASCOM Alpaca client for network device control
alpyca~=3.0.0

# Optional: ZWO camera support (uncomment if using ZWO ASI cameras)
# zwoasi~=0.2.1

# Note: Testing dependencies moved to requirements-dev.txt
